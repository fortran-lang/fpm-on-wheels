[build-system]
requires = [
    "setuptools>=67.0.0",
    "scikit-build>=0.16.7",
    "cmake>=3.20.0",
    "ninja",
]
build-backend = "setuptools.build_meta"

# [project]
# name = "fpm"
# description = "Fortran Package Manager"
# version = "0.1.0"
# authors = [{ name = "The Fortran Programming Language Orgnization" }]
# license = "MIT"

# [project.urls]
# homepage = "https://fpm.fortran-lang.org"
# souorce = "https://github.com/fortran-lang/fpm"
# tracker = "https://github.com/fortran-lang/fpm/issues"

[tool.cibuildwheel.macos]

[[tool.cibuildwheel.overrides]]
select = "*-macosx_arm64"
before-build = "bash {project}/scripts/cibw_before_build_macos.sh {project}"
environment = { FC = "/usr/local/bin/gfortran" }
